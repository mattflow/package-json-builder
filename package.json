{
  "private": true,
  "scripts": {
    "start": "cd client && yarn build && cd ../ && node server",
    "dev": "concurrently \"nodemon server\" \"cd client && yarn start\"",
    "postinstall": "cd client && yarn install"
  },
  "dependencies": {
    "express": "^4.16.3",
    "shortid": "^2.2.13"
  },
  "devDependencies": {
    "concurrently": "^3.6.0",
    "nodemon": "^1.17.5"
  }
}
